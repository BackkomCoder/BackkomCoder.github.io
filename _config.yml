# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: MyCompany 技术博客
email: techblog@mycompany.com
description: >- # this means to ignore newlines until "baseurl:"
  分享 MyCompany 的最新技术动态、研发心得和行业洞见。
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://backkomcoder.github.io/" # the base hostname & protocol for your site, e.g. http://example.com
author: MyCompany Tech Team
twitter_username: MyCompanyTech
github_username:  MyCompany

# Theme (示例，具体取决于您选择的主题)
theme: minima # 如果使用 gem-based 主题，如 minima, chirpy 等

# Build settings
markdown: kramdown
kramdown:
  input: GFM # GitHub Flavored Markdown
  syntax_highlighter: rouge

# 编码设置
encoding: utf-8

# Plugins (示例)
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  # - jekyll-paginate (如果需要分页)

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be ignored by Jekyll.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - README.md
  - LICENSE.md
